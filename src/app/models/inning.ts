// model to store the information of an inning
export class Inning {
  inningNumber: string; // inning number in the game
  home: string;         // home team's runs
  away: string;         // away team's runs
}